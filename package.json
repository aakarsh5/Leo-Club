{
  "name": "leo-club-biratnagar",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "cd client && npm run dev",
    "build": "cd client && npm run build",
    "start": "cd client && npm run start",
    "lint": "cd client && npm run lint"
  },
  "engines": {
    "node": ">=18.0.0"
  }
} 